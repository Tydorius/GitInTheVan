services:
  gitinthevan:
    build: .
    ports:
      - "${GITV_PORT:-8000}:8000"
    volumes:
      - ./data:/app/data
    environment:
      - GITV_SECRET_KEY=${GITV_SECRET_KEY:-}
    restart: unless-stopped
